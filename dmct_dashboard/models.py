# Aucun modele metier propre a cet agregateur : il ne fait que combiner les
# KPI deja calcules par dmct_clients, dmct_demandes, dmct_instruments,
# dmct_prestations, dmct_qualite, dmct_equipements_rh, dmct_inspections et
# dmct_finance (module DMCT).
